{
    IBClasses = (
        {
            ACTIONS = {savePNG = id; toggleCompositionPicker = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {qcView = QCView; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = AppView; LANGUAGE = ObjC; SUPERCLASS = QCView; }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}