{
    IBClasses = (
        {
            ACTIONS = {compositionChanged = id; }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {compositionChoice = NSPopUpButton; qcView = QCView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}