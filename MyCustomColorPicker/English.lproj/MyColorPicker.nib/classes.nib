{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {colorChanged = id; savePrefs = id; showPrefs = id; }; 
            CLASS = MyColorPicker; 
            LANGUAGE = ObjC; 
            OUTLETS = {colorPickerPrefs = NSPanel; colorPickerView = NSView; radios = NSMatrix; }; 
            SUPERCLASS = NSColorPicker; 
        }
    ); 
    IBVersion = 1; 
}